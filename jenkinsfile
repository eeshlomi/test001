pipeline {
	agent any
	stages {
		stage('test') {
			environment { STATE = 0 }
			options { retry(2) }
			steps {
				when { equals expected: 0, actual: env.STATE }
				sh 'STATE=1'
				error ('this is just a test')
			}
		}
	}
}
