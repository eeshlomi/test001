pipeline {
	agent any
	environment { STATE = 1 }
	stages {
		stage('test') {
			options { retry(2) }
			when { not { equals expected: 'skip', actual: env.STATE } }
			steps {
				sh 'STATE=skip'
				error ('this is just a test')
			}
		}
	}
}
