pipeline {
	agent any
	stages {
		stage('test') {
			options { retry(2) }
			steps {
				sleep(time:1,unit:"SECONDS")
				echo "Commit hash is ${GIT_COMMIT}"
				sh "exit `git rev-list --left-right --count origin/master...origin/${GIT_BRANCH}`"
			}
		}
	}
}
