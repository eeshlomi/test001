pipeline {
	agent any
	stages {
		stage('test') {
			options { retry(2) }
			steps {
				sleep(time:1,unit:"SECONDS")
				echo "${GIT_COMMIT}"
				sh   "echo `git rev-list --left-right --count origin/master...origin/${BRANCH_NAME} | awk '{print $1}'`"
				sh   "exit 0"
			}
		}
	}
}
