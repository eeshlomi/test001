pipeline {
	agent any
	environment { STATE = 0 }
	stages {
		stage('test') {
			options { retry(2) }
			when { equals expected: 0, actual: env.STATE }
			steps {
				sh 'STATE=1'
				error ('this is just a test')
			}
		}
	}
}
