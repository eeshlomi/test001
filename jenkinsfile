pipeline {
	agent any
	stages {
		stage('test') {
			steps {
				sleep(time:1,unit:"SECONDS")
				echo "${GIT_COMMIT}"
				sh 'echo `git rev-list --left-right --count origin/master...$(git branch --show-current) | awk "{print $1}"`'
				sh 'exit 0'
			}
		}
	}
}
