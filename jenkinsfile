pipeline {
	agent any
	stages {
		stage('test') {
			options { retry(2) }
			steps {
				sleep(time:1,unit:"SECONDS")
				echo "${GIT_COMMIT}"
				sh 'git rev-list --left-right --count origin/master...branch1 | xargs exit'
			}
		}
	}
}
